[project]
name = "crm-lead-bot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aiogram==3.15.0",
    "httpx==0.27.2",
    "redis==5.2.1",
]

[tool.vercel]
entrypoint = "api.webhook:handler"
